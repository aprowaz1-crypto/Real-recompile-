<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.realrecompile.app"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="28" android:targetSdkVersion="34" />

    <!-- Vulkan GPU required -->
    <uses-feature android:name="android.hardware.vulkan.level" android:version="1" android:required="true" />
    <uses-feature android:name="android.hardware.vulkan.version" android:version="0x00401000" android:required="true" />

    <!-- OpenGL ES fallback not used -->
    <uses-feature android:glEsVersion="0x00030002" android:required="false" />

    <application
        android:label="@string/app_name"
        android:hasCode="true"
        android:allowBackup="false"
        android:theme="@style/RealSteelTheme">

        <!-- Launcher Activity (styled menu) -->
        <activity
            android:name="com.realrecompile.app.LauncherActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="landscape"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Game NativeActivity (launched from menu) -->
        <activity
            android:name="android.app.NativeActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="landscape"
            android:exported="false">
            <meta-data
                android:name="android.app.lib_name"
                android:value="realrecompile" />
        </activity>
    </application>
</manifest>
